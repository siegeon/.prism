# MCP tool usage ledger

One row per registered MCP tool (73 of them), so the question the guide rewrite depends on — *which tools should a reader be told about?* — is answered from evidence rather than from habit.

Generated by `services/prism-service/scripts/gen_tool_usage_ledger.py` (task f1e7e228). Regenerate after changing the tool registry.

## How to read a row

- **Profiles** — which named `tool_profile` surfaces expose the tool. `none` means it is reachable only via `tool_profile=all`.
- **References** — a static scan of the installed plugin (hook scripts, skills, agent specs), the shipped `.mcp.json`, the SPA, the test suite and the docs.
- **Observed** — real calls recorded at the MCP dispatch point (`mcp/server.py`, read back through `/api/tool-usage`). This column went live with this task, so `not yet` on a first pass means the instrumented build has not run long enough, NOT that the tool is dead.
- **Decision** — KEEP interactive / DEMOTE to `tool_profile=all` / RETIRE, each with the evidence it rests on.

**Absence of a reference is *no evidence*, never proof of death.** Removing an MCP verb is a breaking change for every already-connected client, so no row is marked for removal while it is reachable from an installed hook script, a plugin skill/agent spec or a shipped `.mcp.json`.

| Tool | Profiles | References | Observed | Decision | Evidence / what would settle it |
| --- | --- | --- | --- | --- | --- |
| `brain_call_chain` | interactive | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `brain_find_references` | interactive | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `brain_find_symbol` | interactive | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `brain_graph` | admin | docs | not yet | **KEEP** | Referenced by `docs`. |
| `brain_index_doc` | admin | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `brain_list` | admin | none found | not yet | **KEEP** | NO EVIDENCE either way: no inbound reference and no observed call. What would settle it: a `/api/tool-usage` observation after the instrumented build has run a full release cycle; zero calls plus zero references then makes it a removal candidate for the follow-up cleanup task. |
| `brain_outline` | interactive | tests | not yet | **KEEP** | Referenced by `tests`. |
| `brain_search` | interactive | plugin, tests, docs | not yet | **KEEP** | Referenced by `plugin`, `tests`, `docs`. |
| `brain_search_feedback` | learning, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `brain_understand` | interactive | spa, tests | not yet | **KEEP** | Referenced by `spa`, `tests`. |
| `conductor_advance` | none | tests | not yet | **DEMOTE** | Already applied at `mcp/tools.py:1652-1656`: superseded by `conductor_work`, in no named profile, reachable only via tool_profile=all for admin/debug. Removing the verb itself reaches `services/conductor_service.py`, which is in control_plane.POLICY_FILES — that is a separate task tagged policy-change. |
| `conductor_gate` | none | spa, tests | not yet | **DEMOTE** | Already applied at `mcp/tools.py:1652-1656`: superseded by `conductor_work`, in no named profile, reachable only via tool_profile=all for admin/debug. Removing the verb itself reaches `services/conductor_service.py`, which is in control_plane.POLICY_FILES — that is a separate task tagged policy-change. |
| `conductor_work` | interactive | spa, tests | not yet | **KEEP** | Referenced by `spa`, `tests`. |
| `context_bundle` | interactive | tests, docs | not yet | **KEEP** | Referenced by `tests`, `docs`. |
| `graph_annotate_abandon` | learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `graph_annotate_check` | learning, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `graph_annotate_enqueue` | learning, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `graph_annotate_status` | learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `graph_annotate_submit` | learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `graph_rebuild` | admin, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `janitor_abandon` | interactive, learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `janitor_check` | interactive, learning, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `janitor_enqueue` | learning, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `janitor_mark_stale` | learning, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `janitor_status` | learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `janitor_submit` | interactive, learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `memory_invalidate` | interactive, learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `memory_recall` | interactive | spa, tests, docs | not yet | **KEEP** | Referenced by `spa`, `tests`, `docs`. |
| `memory_store` | interactive | spa, tests, docs | not yet | **KEEP** | Referenced by `spa`, `tests`, `docs`. |
| `meta_conductor_auto` | learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `meta_conductor_brief` | learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `meta_conductor_evaluate` | learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `meta_conductor_propose` | learning | tests | not yet | **KEEP** | Referenced by `tests`. |
| `okf_get` | interactive | tests | not yet | **KEEP** | Referenced by `tests`. |
| `okf_graph` | interactive | tests | not yet | **KEEP** | Referenced by `tests`. |
| `okf_index` | interactive | tests | not yet | **KEEP** | Referenced by `tests`. |
| `principles_seed` | interactive | tests | not yet | **KEEP** | Referenced by `tests`. |
| `prism_bulk_refresh` | admin | plugin | not yet | **KEEP** | Referenced by `plugin`. |
| `prism_cancel_pending` | admin | none found | not yet | **KEEP** | NO EVIDENCE either way: no inbound reference and no observed call. What would settle it: a `/api/tool-usage` observation after the instrumented build has run a full release cycle; zero calls plus zero references then makes it a removal candidate for the follow-up cleanup task. |
| `prism_guide` | interactive | tests | not yet | **KEEP** | Referenced by `tests`. |
| `prism_install` | admin | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `prism_onboard` | interactive | tests | not yet | **KEEP** | Referenced by `tests`. |
| `prism_refresh` | admin, automation | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `prism_status` | interactive, automation | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `prism_sync` | interactive | plugin | not yet | **KEEP** | Referenced by `plugin`. |
| `project_create` | admin | tests | not yet | **KEEP** | Referenced by `tests`. |
| `project_list` | admin | tests | not yet | **KEEP** | Referenced by `tests`. |
| `project_onboard` | admin | tests | not yet | **KEEP** | Referenced by `tests`. |
| `record_outcome` | hooks | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `record_session_outcome` | hooks, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `record_skill_usage` | hooks, automation | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `record_subagent_outcome` | hooks, automation | plugin, tests | not yet | **KEEP** | Referenced by `plugin`, `tests`. |
| `register_claude_source` | interactive | spa, tests | not yet | **KEEP** | Referenced by `spa`, `tests`. |
| `task_create` | interactive | tests, docs | not yet | **KEEP** | Referenced by `tests`, `docs`. |
| `task_link_session` | interactive | tests | not yet | **KEEP** | Referenced by `tests`. |
| `task_list` | interactive, automation | tests, docs | not yet | **KEEP** | Referenced by `tests`, `docs`. |
| `task_next` | interactive | tests, docs | not yet | **KEEP** | Referenced by `tests`, `docs`. |
| `task_update` | interactive, automation | tests, docs | not yet | **KEEP** | Referenced by `tests`, `docs`. |
| `understand_bootstrap` | none | tests | not yet | **RETIRE** | mx-0103ae consumer check DONE: the green_gate conformance note reads layers.json straight from `understand_artifact_store` via `conductor_service.py:1772-1788`, NOT through this MCP tool, so the architecture-analyzer pipeline keeps working without it. Superseded by the okf_* Understand wiki. Unreachable from any hook script, plugin spec or shipped .mcp.json. |
| `understand_configure` | none | tests | not yet | **RETIRE** | mx-0103ae consumer check DONE: the green_gate conformance note reads layers.json straight from `understand_artifact_store` via `conductor_service.py:1772-1788`, NOT through this MCP tool, so the architecture-analyzer pipeline keeps working without it. Superseded by the okf_* Understand wiki. Unreachable from any hook script, plugin spec or shipped .mcp.json. |
| `understand_drain_queue` | none | tests | not yet | **KEEP** | Live consumer: `cli/understand_cli.py:198` calls it over MCP. Absence from the plugin scan is not death. |
| `understand_get_domains` | none | tests | not yet | **RETIRE** | mx-0103ae consumer check DONE: the green_gate conformance note reads layers.json straight from `understand_artifact_store` via `conductor_service.py:1772-1788`, NOT through this MCP tool, so the architecture-analyzer pipeline keeps working without it. Superseded by the okf_* Understand wiki. Unreachable from any hook script, plugin spec or shipped .mcp.json. |
| `understand_get_layers` | none | tests | not yet | **RETIRE** | mx-0103ae consumer check DONE: the green_gate conformance note reads layers.json straight from `understand_artifact_store` via `conductor_service.py:1772-1788`, NOT through this MCP tool, so the architecture-analyzer pipeline keeps working without it. Superseded by the okf_* Understand wiki. Unreachable from any hook script, plugin spec or shipped .mcp.json. |
| `understand_get_onboarding` | none | tests | not yet | **RETIRE** | mx-0103ae consumer check DONE: the green_gate conformance note reads layers.json straight from `understand_artifact_store` via `conductor_service.py:1772-1788`, NOT through this MCP tool, so the architecture-analyzer pipeline keeps working without it. Superseded by the okf_* Understand wiki. Unreachable from any hook script, plugin spec or shipped .mcp.json. |
| `understand_get_tour` | none | tests | not yet | **RETIRE** | mx-0103ae consumer check DONE: the green_gate conformance note reads layers.json straight from `understand_artifact_store` via `conductor_service.py:1772-1788`, NOT through this MCP tool, so the architecture-analyzer pipeline keeps working without it. Superseded by the okf_* Understand wiki. Unreachable from any hook script, plugin spec or shipped .mcp.json. |
| `understand_refresh` | automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `understand_status` | automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `understand_store_result` | none | tests | not yet | **KEEP** | Live consumer: `cli/understand_cli.py:212` calls it over MCP. Absence from the plugin scan is not death. |
| `verifier_feedback_summary` | hooks | none found | not yet | **KEEP** | NO EVIDENCE either way: no inbound reference and no observed call. What would settle it: a `/api/tool-usage` observation after the instrumented build has run a full release cycle; zero calls plus zero references then makes it a removal candidate for the follow-up cleanup task. |
| `verifier_history` | admin | none found | not yet | **KEEP** | NO EVIDENCE either way: no inbound reference and no observed call. What would settle it: a `/api/tool-usage` observation after the instrumented build has run a full release cycle; zero calls plus zero references then makes it a removal candidate for the follow-up cleanup task. |
| `verifier_run` | admin, automation | tests | not yet | **KEEP** | Referenced by `tests`. |
| `workflow_advance` | none | tests, docs | not yet | **DEMOTE** | Already applied at `mcp/tools.py:1652-1656`: superseded by `conductor_work`, in no named profile, reachable only via tool_profile=all for admin/debug. Removing the verb itself reaches `services/conductor_service.py`, which is in control_plane.POLICY_FILES — that is a separate task tagged policy-change. |
| `workflow_state` | none | plugin, tests, docs | not yet | **DEMOTE** | Already applied at `mcp/tools.py:1652-1656`: superseded by `conductor_work`, in no named profile, reachable only via tool_profile=all for admin/debug. Removing the verb itself reaches `services/conductor_service.py`, which is in control_plane.POLICY_FILES — that is a separate task tagged policy-change. |

## Roll-up

- KEEP: 63
- DEMOTE to `tool_profile=all`: 4
- RETIRE: 6

The surface shrinks: 10 of 73 tools are marked to leave the default surface. Acting on the RETIRE rows is deliberately a FOLLOW-UP task, not this one: the four DEMOTE rows are the superseded conductor/workflow drive verbs, and deleting those reaches `services/conductor_service.py`, which is in `control_plane.POLICY_FILES` and must be split into a task tagged `policy-change`.
