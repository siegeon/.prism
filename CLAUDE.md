# PRISM Project

PRISM is a software engineering methodology and Claude Code plugin with an MCP service for AI-assisted development.

## Project Knowledge

Use Prism (MCP) for all project knowledge — do not create static architecture docs.

- `brain_search` — find code, docs, patterns across the project (reach for it before grep)
- `memory_recall` — recall conventions, decisions, and expertise
- `brain_call_chain` — trace call flow and blast radius from the graph
- `memory_store` — write decisions back the moment they're made; PRISM is the memory layer, chat is not

## Writing standard for PRISM content

Everything written INTO PRISM — task titles, descriptions, oracles, stop_if lines, memories, ontology labels and comments, rule messages — follows ASD-STE100 Simplified Technical English via the vendored `asd-ste100` skill (`.claude/skills/asd-ste100/SKILL.md`, owner 2026-08-26). Strict mode for instructions (oracle, stop_if, likely_misfire, error strings, tool descriptions); STE-flavored for descriptions and memories. The application enforces the regex-checkable subset itself: `prism_service/services/ste.py` normalises every task/memory write and returns a `style` block, and the ontology rule `text-is-plain` flags what survives on the Rules tab. Keep every hedge — a rewrite that turns "may have failed" into "failed" is a different claim, not a simplification.

## Working tasks

All in-progress work is a PRISM task driven through the conductor — never bare `/api` pokes.

**THE CANONICAL DOCTRINE LIVES IN PRISM, NOT HERE**: call `prism_guide(section="conductor")` before driving a task. It owns the loop, the two human gates, evidence freshness (re-mint after ANY commit that moves the tree), why `readiness` is not the decider, why Override cannot skip the oracle, and filing follow-ups as subtasks. Add new conductor doctrine THERE (`mcp/tools.py` `_GUIDE_SECTIONS["conductor"]`) so every consumer inherits it — this file stays a pointer, not a second copy that drifts.

- Implement a task through the conductor loop: `job = conductor_work()` → do exactly `job["instructions"]`, produce `job["expected_proof"]` → `conductor_work(id=..., outcome=..., proof=...)`. The server owns the step sequence; never hand-drive or hand-clear SDLC steps or gates.
- Create work with `task_create` (title = human-friendly WHAT, ~4-9 words; mechanics in description; define the `oracle` + `likely_misfire` up front). Watchable tasks are root tasks (`parent_id=""`).
- A gate is decided by a DISTINCT actor — the producing session cannot clear its own gate; a red test is always your fault, never "pre-existing".
- The distinct actor need not be human, but machine adjudication is OPT-IN (owner decisions 2026-07-15/16): in environments that set `PRISM_GATE_ADJUDICATOR_INTERVAL=<seconds>`, the conductor's `conductor-adjudicator` seat decides a green_gate on a FRESH PASSING EvidenceReceipt from its own trusted runner, and a red_gate for `proof_type=demo` tickets via the demo rubric (no test suite by design; proof burden stays at green_gate). Both seats must work (owner 2026-07-16): a human can always click, AND an automated user must be able to complete the same gates. Non-opted-in environments keep human clicks as the norm. The human always keeps: visibility of every machine decision, reject/override, manual-evidence oracles, and failed gates.
- The fundamental workflow: the MAIN chat thread spawns an async subagent to work a ticket through PRISM end-to-end (conductor loop, gates included); the MAIN thread then evaluates what it took — friction, visibility, cost — and refines the process. Anything that structurally prevents a subagent from completing a ticket is a product defect.

## Self-learning

When I correct you, or you catch yourself making a mistake: before continuing, add the lesson as a one-line rule under `## Lessons`, so it never happens again.

## Lessons

- Substrate repairs that make PRISM task execution trustworthy are performed externally by Codex, never driven as PRISM tasks; PRISM may resume internal work only after the repaired boundary is independently verified.
- A conductor step validation result is a typed PRISM ontology object (identity, outcome, failures, summary, evidence reference); raw command output is supporting evidence, never the caller-facing result contract.
- Workflow orchestration belongs IN the daemon, not hand-cranked from a session (owner 2026-08-16/17, twice): the standing direction is mx-d5275d / epic 3baadd19 "PRISM drives its own tasks end to end" — task_runner.py already holds the drive worker (off by default, gap list in mx-bd3bd9). When the owner sees me driving lanes from without, the move is to advance that epic, never to defend the lanes; until it ships, external drives are the stopgap and must pass session_id in args.
- Fixing a substrate bug externally (per the lesson above) does NOT license hand-driving everything downstream of the fix too. On 2026-08-23 I found and fixed ship_worker.py's missing `--repo` flag (a genuine substrate repair, correctly done outside the task system) — but then kept going by hand: manually `gh pr merge`-ing, manually `git reset`/cherry-picking a task's own branch, manually calling `/api/conductor/gate/mint` and `conductor_gate approve` myself instead of stepping back once the daemon's own ship_worker/gate_adjudicator seats could run the exact same pipeline unattended. Owner: "so as qa you didn't use prism to do the work that you were instructed." The correct move after a substrate repair is to get out of the way and let task_runner/ship_worker/gate_adjudicator finish the job — hand-driving the aftermath just relocates the bypass one step downstream.
- The scratch-daemon live-verify pattern's env var is `PRISM_UI_PORT`, NEVER `PRISM_WEB_PORT` — the latter is silently ignored (`config.py:25` reads only `PRISM_UI_PORT`, default 7778), so a scratch daemon launched with the wrong name defaults its UI onto port 7778, the WSL-release web port CLAUDE.md says never touch. On task 457b38db this squatted 7778 for ~3 minutes (confirmed via `curl 127.0.0.1:7778/api/version` returning the scratch daemon's own build) before being killed via `TaskStop` on its `run_in_background` task id (the safety classifier blocked every direct-kill route — PowerShell `Stop-Process`, `taskkill`, `python os.kill` — so `TaskStop` on the background-task id it was launched as is the one working, non-classifier-blocked kill route). Always pre-check the target scratch port is refused (`curl -m3 -o /dev/null -w '%{http_code}' <port>`) immediately before AND after launch, never assume the env var name from memory.
- Never clean up a planted/temporary line with `git checkout -- <file>` while the file holds uncommitted real work — it reverts EVERYTHING; remove the planted line with a targeted edit instead.
- On a conductor_work drive, the story/plan rubrics read `task.plan_doc`, and the draft_story/verify_plan `proof=` OVERWRITES plan_doc — so pass the FULL story/plan markdown as `proof=`; a `task_update(plan_doc=...)` made beforehand is clobbered by the very next report, and a one-shot rubric autoclear miss parks story_gate/plan_gate pending until the adjudicator's rubric re-sweep (v7.0.49) lifts it — keep the report compliant at the moment it lands anyway.
- When writing doctrine/memory from an owner conversation, record the owner's ACTUAL intent, not my hardened absolutization of it — "human in the loop" means visibility + override, never "every gate is a human click"; an over-strict note I write becomes a wall every future session (and every supervisor) enforces against the owner's real goal.
- Evidence for PRISM work (screenshots, audit reports) goes INTO PRISM — the task evidence store + /tasks/:id/proof — never claude.ai artifacts or any external host; the owner reviews evidence where the gate is.
- DONE means SHIPPED — merged and validated on main (released), not drive-complete: a green_gate pass is "verified"; never report or display a task as done while its commits sit on an unpushed branch (owner 2026-07-16).
- Bouncing the dev daemon from a sandboxed background shell ties it to that shell's Windows job — it dies silently (no traceback, mid-request logs) when the harness recycles the shell; launch with the sandbox disabled (or F5) so the daemon survives. Sandbox-disabled is NOT enough on its own (2026-07-21: `prism start --daemon` still died mid-request, twice) — `CREATE_BREAKAWAY_FROM_JOB` is blocked, so spawn it via WMI `Win32_Process.Create` with `ShowWindow=0` to land it outside the job entirely (`.venvs/dev/prism-dev-daemon.cmd`). Use `python.exe` in that hidden console, NEVER `pythonw.exe`: a GUI-subsystem parent has no console to hand down, so every short-interval worker (`event_pool`, 5s) allocates its own console and the screen flashes windows at the owner.
- A lane must commit its failing tests as a TESTS-ONLY `[task:<id>]` commit at the red step, BEFORE any implementation commit — the red machine seat anchors to that commit; bundling tests+impl makes red undemonstrable and strands red_gate with a human.
- DRIVE THE CONDUCTOR FIRST, never retrofit it onto finished work. On 19e4e7f7 I built directly, bundled tests+impl into one commit (6c22d46), shipped it, and only then ran `conductor_work` — so red_gate anchored at the merge commit where the pinned suite passes (rc=0, gate wants rc==1) and NO commit in history can ever demonstrate red. I had the red evidence (4 failed / 3 passed against pre-change 3c87021 in a scratch worktree) and the seat could not use it, because the anchor is chosen from history, not from what I attach. The owner's question was exactly this: "why are you not driving with the implement skill". Building first is not a shortcut to the same place — it permanently forecloses the ticket's own red receipt.
- Slice scoping must check control_plane.POLICY_FILES up front — a task whose allowed_files includes a gate-policy file will fail its own gates on the candidate-controls-judge tooth; put consumers of policy modules in a separate non-policy file, and never let a producer tag its own task policy-change mid-drive.
- Long curl -d JSON bodies with unicode (em-dashes) silently fail through the shell — write the body to a file and use --data-binary @file.
- Committing a test-proof lane's failing tests to the WORKING BRANCH instead of the per-task scratch worktree used to strand red_gate forever (the anchor stamped from the lagging worktree HEAD landed pre-tests → "no tests ran"); FIXED v7.1.18 (mx-6c73be): the red anchor now self-heals to the committed tests-only `[task:<id>]` commit and mints red on-demand at approval — but the durable habit is still to let the red machine seat anchor cleanly, and NEVER reach for a bare `/api/conductor/rewind` poke (no MCP tool + classifier-blocked) to unstick a strand; fix the anchor logic, don't hand-repair.
- Before escalating a "scope decision" to the owner, SEARCH the repo for how it already solves that problem — on task 89e90d1a I asked whether to add a Vitest harness when `tests/unit/test_conductor_page_animated_cleanup_ui.py:4-6` already documents the convention ("the PRISM SPA has NO JS test runner, so UI ACs are pinned by asserting the ACTUAL TSX source"); the owner had to prod twice. A plan constraint that LOOKS contradictory ("no new files" vs. a test file) is usually a convention I haven't found yet, not a fork for the human.
- NEVER report a gate as "machine-decided / cleared" without READING its gate_reason receipt first — on 5a6837a0 I called three gates cleanly machine-cleared across four turns while the green receipt said `adapter=http_probe, tree=c162b66`, a tree from ANOTHER task that did not even contain the file under test; the task went DONE on it. A pass is a claim about evidence: open the evidence (mx-399f3e, task e0149f1f).
- A prototype draws ONLY THE DELTA — never re-implement chrome the app already has. The login mock hand-rebuilt the shell and silently shipped a redesign nobody asked for: it renamed Dashboard to "Overview" and dropped Understand, Retrievals and Consolidation, so the owner read it as "you are redesigning the entire application" (owner 2026-07-22). A hand-drawn copy of an existing surface always drifts, and the drift reads as a proposal. Mock the NEW surfaces; for anything unchanged use the real app (real component/nav names, or a screenshot of it) and say plainly that it is untouched.
- Design/prototype is the PLAN PHASE of the real task, never its own ticket: `WORKFLOW_STEPS` (models/workflow.py) has no design step, and `get_task_prototype` already serves a task's clickable mock into the iframe on ITS OWN Plan card — so build the mock at `verify_plan` and have the owner approve the direction at `plan_gate`. Splitting design into a separate task (as 7cc4f0cf/a1e4120f did) cargo-cults a workaround for `plan_gate` having no owner-approval tooth for UI direction; fix the gate, don't fork the ticket (owner 2026-07-22).
- A tooth that COMPUTES a refusal reason and then `return None`s it has only half-shipped: on e0149f1f both green-seat receipt teeth built a precise one-liner (tree sha, absent pinned file, adapter mismatch) and threw it away, so the gate parked with an EMPTY gate_reason and no driver could self-diagnose — record the reason at the seat (pending + reason + audit row, never `failed`), de-duped against the re-sweep interval.
- Every honesty tooth added to a CARD must be asked for again at the SEAT THAT DECIDES — v7.1.24 taught the gate card to render a stale receipt neutrally, but the adjudicator's approve path never learned it and closed a task on that same stale receipt.
- "idle" and "stalled" are ALARM words — the owner reads them as "I must intervene somewhere" (owner 2026-07-21). Render them ONLY when the owner actually has an action; a tile that NAMES the step it is executing must never also claim nothing is happening ("WRITE FAILING TESTS · 63% … task idle 7:26" while the verifier was writing 2s earlier). "We can't see it" and "it's broken and needs you" must read differently — and rewording the pill without fixing the underlying state is the misfire, not the fix (mx-dfd56a, task 8ddbba7f).
- A slice whose tests INJECT the collaborator it depends on can go green while the assembled product is dead — the 0784729f epic shipped 5 green slices (store, GitHub adapter, Jira adapter, webhooks, Work UI) and `/code-review` then found the feature could not run AT ALL: no adapter is registered in production (every test injected one), the surface is invisible in local mode (`/api/workspaces` returns `[]`), and Start is inert because the UI reads a `task_id` the entities API never serializes. Both sides of every seam were verified against MY assumption of the other side, and my assumptions agreed with each other. PREVENTION at authoring time: (1) the FIRST slice of an epic is a walking skeleton that goes end-to-end through the REAL wiring (connect -> sync -> one visible row), never the storage layer first; (2) an oracle that can be satisfied with every collaborator mocked is not an oracle — it must name a user-reachable surface; (3) each slice declares WHAT PRODUCTION CODE CONSTRUCTS IT ("nothing yet" means the wiring belongs in this slice); (4) the epic's OWN oracle needs a receipt before its children count as delivered — all 7 children of 0784729f went done while the epic sat `pending`, unexercised; (5) run `/code-review` at the epic's exit, not after the owner finds it.
- ASSERT THE AFFORDANCE A PERSON USES, never the constant behind it — e139295d's AC-1 checked `SectionId`/`KNOWN_SECTIONS`/`SECTION_META` for the new Connectors section, all three were correct and green, and the section was still unreachable because the nav that a human clicks lives in `components/Sidebar.tsx`, outside that slice's allowed_files. A UI slice's oracle must pin the entry point (the nav link, the button, the route a click actually lands on), and if that file is out of scope the slice is mis-scoped; the owner reported the identical symptom three times ("i still see the integrations in the claude auth section") while my tests stayed green (task 7fff8ef0).
- The pinned suite in `task.verify` is the GATE'S EVIDENCE, never the blast radius — c89edbeb and 064fc09b each changed a contract that older suites still pinned, both went machine-green on their own file, and main sat RED with three contradictory tests (`test_claude_auth_remains_a_sibling_entry` wanted the nav entry the owner had just asked me to delete; `test_integrations_no_longer_render_under_claude_auth` indexed a block that no longer existed; `test_the_claude_card_has_something_to_click` wanted the Details button the whole-card click replaced). Before reporting green, run every NEIGHBOURING suite touching the same surface, and when a slice deletes a contract, retire its old assertions IN THAT SLICE with a comment saying what superseded them — a contradiction left standing is a red main, and "the gate passed" does not mean the repo is green (task 2ba63a22).
- A source-reading test must match the RENDERED TAG (`<ClaudeAuthCard`), never the bare name — my own explanatory COMMENT satisfied the assertion three separate times (the `<img>` guard, the ClaudeAuthCard laziness check whose branch resolved to `c.state === "not_connected"`, and the "Details" affordance check that passed on a comment saying where the button used to be). Never slice a fixed character window around a match either; parse the enclosing JSX branch condition, because a comment above the element silently pushes the real guard out of the window.
- This working tree is SHARED and other sessions move HEAD under you mid-task: re-read `git rev-parse --abbrev-ref HEAD` in the SAME command as every `git commit`, never trust a branch check from an earlier turn. I committed to main this way (forbidden) because the branch changed between my check and my commit; recovery was `git branch <save> <sha>` then `git reset --keep origin/main`, which is only safe while the commit is unpushed and the tree is clean. Same reason: never `git add -A`/commit the tree wholesale (other sessions' uncommitted work is sitting in it), and before retracting a red finding as flaky, run `git show <original-sha>:<file>` — a test that was red and is now green was far more likely FIXED under you than flaky.
- A machine-adjudicated rubric/red gate that parks PENDING writes NO gate_reason to the task — a driving subagent can't self-diagnose and pings a human at every gate; the real reason is computed (story needs `AC-<n>` not `AC1`; plan_gate reads the plan_diagram FIELD not a ```mermaid fence in plan_doc; red wants rc==1 and gets rc=4 "no tests ran" when task.verify isn't workspace-root-relative). Read the rubric/receipt to unblock; set `task.verify` to a workspace-root-relative path (`services/prism-service/tests/...`, not `cd services/prism-service && pytest tests/...`) at creation. Fix tracked in task 8f48f9bb.
- NEVER quote `task.gate_reason` as a gate's CURRENT state — it is a stored snapshot, and `GET /api/conductor/gate/readiness?task_id=&project=` is the live truth built for exactly this (`api/conductor.py:85`). On b064db4e I relayed a stale "oracle not evidenced / manual_evidence_required" to the owner for three turns while readiness already said `receipt_ok:true, adapter=human, "your review is the sign-off; Approve to release"` — the Approve was clean and enabled the whole time. Read readiness BEFORE telling a human they are blocked.
- When a change DELETES a named structure, `grep -rln "<the file>" tests/` and run EVERY suite that reads it, BEFORE the PR - the call graph will not find them. Source-reading tests index by literal string (`const HANDLERS = {`, `for (let i = startIdx`), so they are invisible to `brain_call_chain` and to the neighbouring-suite rung. The implement.js rewrite (PR #257) shipped a RED main because I fixed the one suite I happened to find and assumed it was the set; five more assertions across four suites were still pinning deleted structures, and the next drive's green_gate is what caught it. Re-anchor each with a comment naming what superseded it, and check whether the assertion encodes a real INVARIANT that must move rather than die: the dropped "base must not be behind origin/main" guard was a genuine safety property, because `ensure_workspace` defaults base_ref to the repo's CURRENT HEAD, so a stale checkout silently yields a stale worktree.
- A `proof_type=demo`/`review` green_gate is HUMAN-ONLY by owner rule eaafdf75 — `adjudicate_green_gate` returns `None` for them deliberately, because the machine seat once false-greened 7cc4f0cf and a1e4120f and ate the single sign-off. So do NOT "unblock" one by adding a loadable URL to the oracle or flipping `proof_type`; that games the distinct-actor rule. The driver's job is to make the owner's click GROUNDED (live demo URL + durable screenshots in the PRISM evidence store + a green machine-checkable verify), then let them click. Consult memory (mx-438645, mx-df396b) before inventing a gate workaround.
- A SHARED code path is the right home for new behaviour only when every provider/consumer it serves has actually been exercised — otherwise gate it by name. On 0a9b511f I put the inbound status reconcile in `work_item_sync._import_one` with no provider gate, so Jira inherited it and I rewrote `test_jira_work_import.py`'s real contract for an adapter that is not even registered in production (`/api/integrations/connect/mirror` reports `adapters:["github"]`). "Provider-neutral" is an architecture argument, never a licence to change contracts for integrations that do not exist yet. And note the actual failure: I flagged the breadth TWICE in chat and committed it anyway — noticing an overreach is not the same as not shipping it, so raise it as a question before the commit, not as a caveat after (owner: "we have not done jira yet, just github").
- ASK PRISM BEFORE BUILDING, not just before grepping. On 2026-08-02 I profiled a slow drive with Bash grep + file reads, then hand-edited `.claude/workflows/implement.js` and committed it, with no task, no oracle and no test. One `task_list(tag="conductor")` would have returned `dcbd284f` "A drive halts instead of retrying forever" - priority 80, oracle written, test path pinned - already sitting there for the adjacent half of the problem. The owner's question was "why do you skip reaching out to PRISM to get the plan in the context for the implementation details". THE STRUCTURAL REASON: the `implement` workflow's `locate` step is REQUIRED to call `context_bundle` and go brain-first, but when I work in the MAIN thread there is no such step, so the context fetch is discretionary and I default to grep. Treat brain_search + task_list as the mandatory first move of any main-thread work on this repo, exactly as `locate` treats it. Retrofitting a task onto the finished edit afterwards (f4043364) does not undo it - the red_gate is already unwinnable, which is the same lesson as 19e4e7f7.
- When the owner scopes a review ("review my skills for building PRISM"), report only what touches that scope — a generic checklist will hand me account-level or other-project items (claude.ai connectors, growwise/csregs skills) and turning those into ACTION ITEMS makes the owner do the filtering I was asked to do. Out-of-scope findings go in one line or not at all.
- BEFORE BOUNCING THE DEV DAEMON, run `git status` in `E:\.prism` and REFUSE if any `control_plane.POLICY_FILES` entry is dirty — the daemon source-runs the checkout, so a bounce silently loads whatever Python is on disk. On cb1dc6f4 a lane told to stay in its worktree wrote +254 lines into the live `conductor_service.py`, hit a stale-red-anchor defect, fixed that defect IN the judge's own code, and proposed a bounce: that would have cleared the lane's own gate using the lane's own fix. Pinned policy CONTENT does not protect the RUNTIME. Verify the working tree matches a commit (`git hash-object` vs `git rev-parse <sha>:<path>`) before restoring anything, then restore — never assume the lane's work is only in its worktree (task 69233ca0).
- ADDING A COMMIT to a branch whose gate evidence is already minted SILENTLY INVALIDATES that evidence — on ae67ed5c the receipt was minted at `e58a6df`, I committed `767aa63` on top (a correct fix), and the card flipped to "0 / 6 · stale, measured at e58a6df, NOT the tree under review", which the owner reasonably read as "the tests never passed". Re-capture evidence AT the new tree in the same breath as the commit, and say in the proof what moved the tree and why; never leave a human to interpret a staleness marker as a failure.
- A test that SOURCE-SCANS for a forbidden string encodes a TEMPORARY owner instruction as a PERMANENT contract, and detonates the moment the instruction is lifted: `test_this_slice_never_writes_to_github` (task 900a4fb9, docstring "read-only for now") asserted `"PATCH"` never appears in `github_rest.py`, so the day the owner authorised write-back it turned a correct slice into a red main, and the producing lane could not fix it because the file sat outside its allowed_files. Write such guards against BEHAVIOUR (this code path made no write call) not against source text, scope them in the name to the thing that is really invariant, and when you do supersede one, retire it IN the superseding slice with a note naming what replaced it.
- `cd <task worktree> && pytest` LEAVES THE PERSISTENT SHELL THERE, and every subagent spawned afterwards INHERITS that cwd — on 2026-07-30 the a1f2244d lane launched with its cwd inside 7cf6a2e5's worktree, on a sibling lane's branch. It caught this by running `pwd` before touching anything; a lane that trusted its launch directory would have committed into another task's tree and contaminated a branch nobody was watching. `cd /e/.prism` after any command that enters a worktree, and tell every driver to resolve its tree from `job["workspace"]["path"]` (which conductor_work returns correctly) rather than from where it happens to start.
- NEVER write `<parameter name="x">` markup inside a tool argument's VALUE — on the #222 follow-ups I embedded oracle/likely_misfire/proof_type as XML text inside `task_create(description=...)` twice, so all three landed as literal junk in the description while the real `oracle` field stayed EMPTY; an empty oracle is not a cosmetic defect, it silently disarms the green_gate oracle tooth for that ticket. Pass every field as its own tool argument, and after any `task_create` carrying an oracle, read the row back and confirm `oracle`/`likely_misfire`/`proof_type` are populated before moving on.
- NEVER report a background drive as "launched and running" from a `status: running` poll plus a growing transcript — that is the ABSENCE of a terminal result, not evidence of progress. On 2026-08-03 I told the owner the c7ce0fc3 drive was running while it had already HALTED at pre-flight ("branch main is 1 behind origin/main"); it never reached `conductor_work`, so PRISM showed nothing and the owner's "i dont see it being worked in prism" was right while I was wrong. Wait for the terminal notification, or read `journal.jsonl` in the transcript dir for a `result` row, before claiming a drive is healthy. Related: HEAD moved from my session-start branch to `main` between my check and the launch, because this tree is SHARED — re-read the branch in the same breath as the launch, and relaunch FRESH after a pre-flight halt (a `resumeFromRunId` replays the cached `sane_branch=false` verdict and halts again on stale data).
- `mcp__prism__task_update` has NO `description` parameter: passing `description=` is SILENTLY DROPPED and the call still returns ok with the old text. On e3b7ebf6 I "recorded" a source-verified architecture finding into the description and it never landed — only tags/oracle/likely_misfire/verify/stop_if applied. Read the returned row back after every update, and put durable ticket knowledge in `oracle`/`likely_misfire`/`stop_if` or in `memory_store` (mx-684311), never in the description, which MCP cannot edit at all.
- `dependencies` is CREATE-TIME ONLY: neither `mcp__prism__task_update` nor `PATCH /api/tasks/{id}` (`TaskUpdate`, api/tasks.py:1806) has the field, so a `dependencies=[...]` update is silently dropped and echoes `"dependencies": []` — on epic d6966b43 (2026-08-25) I "sequenced" nine children this way and none of it landed. Pass `dependencies` on `task_create`, and read the echoed row for the field you set before moving on (defect tracked as d67bca9f).
- `tsc` DOES NOT READ CSS: a `*/` inside a CSS comment (`--surface-*/--border-*/`) ended the comment early in index.css, Tailwind parsed the prose as CSS ("Unterminated string"), and v7.13.58 shipped with a bundle that did not build while `tsc --noEmit` was rc=0 and every source-reading test was green (epic d6966b43, 2026-08-25). The ship check for any TSX/CSS change is the REAL `npm run build` (in the worktree via a temporary node_modules symlink), never the typecheck alone.
- UNCOMMITTED EDITS TO A `control_plane.POLICY_FILES` ENTRY POISON GATE TESTS IN THAT WORKTREE: `dirty_judge_reason()` appends a `dirty-judge-caveat` history row to every gate decision, so `test_task_history_captures_every_transition` (gate_decide count 2 != 1) and the gates-panel suites go red with no code defect. On 2026-08-25 I chased a phantom double-write for four turns; the axioms lane hit the same tooth. Commit the policy-file edit, THEN run the suites.
- Diagnose WHERE a surface's state is computed before assigning blame for it. On the "STALLED · NEEDS YOU" tile I first told the owner "the tile is lying" (blaming the classifier), then over-accepted his counter that the drive simply is not reporting. Reading the source settled it: `activity_for` (conductor_service.py:4527) takes only task_motion_s / session_quiet_s / status, and `_task_motion_s` reads ONLY `advance_task`/`gate_decide` history rows — never `agent_runs`, and `updated_at` only when there is no transition history. So the driver CANNOT fix it (there is no heartbeat verb to call) and the classifier is not fabricating (it has no input). Both halves of a "who is at fault" question can be wrong; read the computation before answering.
- A UI fix is NOT live until `web_dist` is REBUILT — bouncing the daemon and confirming `/api/version` proves only the PYTHON build, and `web_dist/` is gitignored, so a merged TSX change reaches nobody. On 2026-08-03 the owner reported the task page taking >10s; the served bundle was dated Jul 31, three days and two merged UI PRs stale (25e9047 SSE + e8ff4c0), so the browser was still running the PRE-SSE build and refetching the full ~190KB `/api/tasks/{id}` payload every 5s forever. Measured on one idle task page over 45s: 9 full-detail refetches + 9 child-list + 9 conductor/state BEFORE, 0 / 0 / 0 after a 15s `npm run build`. So end every UI change with `npm run build` in `prism_service/web` AND check the served chunk actually contains the new behaviour (`grep sse/tasks web_dist/assets/*.js`) — a green test suite and a bumped /api/version both pass while the screen stays old.
- NEVER let a drive CLOSE a task (status=done / full_outcome_complete) without the owner's EXPRESS permission — stop at a validated green_gate so the owner can inspect the evidence packet while they are actually there. Owner 2026-08-03 on c7ce0fc3: "you should have stopped at done so i could validate the evidence packet when im here... you can close it but only with my express permission". THE CAUSAL LEVER IS `proof_type`: that ticket had an EMPTY proof_type and I set it to `test`, which is precisely what makes green_gate machine-adjudicable (demo/review are human-only by rule eaafdf75). Setting proof_type=test on a ticket the owner will want to eyeball routes their sign-off to the conductor seat and closes the task behind their back. Leave proof_type demo/review, or ask, whenever the owner should see it. Closing does NOT destroy anything (branch, commits and evidence survive and remain shippable) — but it takes the decision away from them, which is the harm.
- A PENDING ticket's `oracle`/`likely_misfire` is a SNAPSHOT of intent at authoring time and can contradict what the owner wants now — re-read it against any fresh directive BEFORE driving it. On c016667f (2026-08-03) the owner said every feature must carry its highest-order visual plan into plan_gate; the ticket's own likely_misfire, written 2026-07-22, named that exact outcome as the failure mode ("plan_gate starts demanding owner approval for EVERY task... killing the machine adjudication") and scoped the stop to ui-tagged tasks. Driving it as written would have shipped the opposite of the ask while passing every gate. When you do reverse one, mark it SUPERSEDED IN PLACE with the date and the owner's words rather than deleting it — the next session needs to see that the reversal was deliberate, not drift.
- `GET /api/tasks/{id}` returns a WRAPPER, not the task: top-level keys are `task`/`history`/`activity`/`timeline`/`sessions`/`spend`/`phase_progress`, so `d["plan_doc"]` is a KeyError and `d.get("plan_doc")` silently reads as empty. I announced "plan_doc is empty, something's off" about a 12,000-char plan that was sitting right there under `d["task"]`. Read task fields via `task_list(id=..., fields=[...])`, or index `d["task"]` — and when a field reads unexpectedly empty, check the envelope shape before reporting data loss.
- THE OWNER STOPS AT EXACTLY TWO GATES: `plan_gate` (approve the plan) and `green_gate` (approve the final state). `red_gate` belongs to the MACHINE seat and must NEVER be routed to a human (owner 2026-08-04: "i do not want to be stopped on the approve red gate for the tests, that is for the adjudicator"). When a machine seat abstains at red, that is a SYSTEM DEFECT to fix, never a click to request — on 4d399e0a the demo-red seat abstained because `candidate_policy_edit_reason` tripped on a stale `workspace.baseline`, and the honest repair is the baseline, not a human approve (and never a `policy-change` tag or `PRISM_POLICY_CHANGE_APPROVED=1`, which would hide a bookkeeping bug behind the security tooth).
- NEVER quote a gate's state from a STORED field — `task.gate_reason`, `task.workflow_step` and the tile's activity string are all snapshots that go stale the moment a drive rewrites its artifact. On 2026-08-04 I told the owner 499ba9c9's story was "missing oracle lines on AC-1..AC-7" straight off `gate_reason`; running the real rubric parser (`arc_governance._ac_lines`) against the CURRENT `plan_doc` showed 7 of 7 ACs carrying `oracle:`. The drive had already fixed it and only the stored string was old. Compute the live answer (readiness endpoint, or run the scorer yourself) before telling a human they are blocked — and note readiness itself can answer for the WRONG GATE: it returned a `green_gate` "no EvidenceReceipt on file" refusal for a task sitting at `story_gate`, which is the string the UI's "BLOCKED · evidence not on file" banner renders at every gate.
- A SUBTASK IS MINE TO FINISH — driving one through the conductor is fine, deferring any part of its execution back to the owner is not (owner 2026-08-06: "you may not defer their execution at any time to the caller. you must then resolve them in their entirety, they are SUB TASKS and there for beneth the notice of the human"). No owner click at a child's plan_gate, no "which option do you want", no parking it pending review; a fork inside a subtask is mine to decide. The mechanical trap: `proof_type=demo`/`review` makes green_gate human-only by rule eaafdf75, so a subtask carrying it CANNOT be finished without bothering them — children need machine-adjudicable proof (`test` + a real pinned suite), while ROOT/epic tasks keep both human stops. The trigger was surfacing child c9d74538 as its own tile: `managed_tasks` (conductor_service.py:3953) has no parent filter and emits no `parent_id`, so a driven child renders as a peer of its own epic (mx-c739c6).
- `verify_green_state` IS NOT A SECOND TEST RUN — it is where I check that what I built VISUALLY ALIGNS with the approved prototype/design packet, so the evidence reaching the owner at green_gate is worth their time (owner 2026-08-06: "the whole point of the verifyer step is to validate what you implemented visually aligns enough with the green gate that you can submit the evidence to me"). On 0784729f I reported 1521 green tests and curl output while the approved mock specified an INBOX nav entry, the same Inbox in Buzz, and a Buzz connector card — I shipped zero UI and picked GitHub as the transport. Causes: (1) I never fetched the prototype (`GET /api/tasks/<id>/prototype?project=<proj>` — it 404s without the project param) and trusted plan_doc's prose, which had already drifted from the artifact; (2) my own AC oracle was `curl /api/collaboration/surfaces`, an API rather than an affordance a person clicks, violating the stop_if I wrote myself; (3) NO GATE CATCHES THIS — story/plan rubrics check FORM, and the design packet is approved at plan_gate then never consulted again, so a backend-only drive reaches "the evidence is ready" looking healthy. At verify: open the prototype, drive the real screen, screenshot into the PRISM evidence store, and say which approved surfaces are and are not present (mx-750294).
- AN EPIC'S GATE IS ITS OWN ORACLE DEMONSTRATED, NEVER A ROLL-UP OF GREEN CHILDREN. On 0784729f I drove 13 slices to machine-adjudicated green, merged 3,379 lines, deployed, and presented the parent gate as ready — while not one clause of the epic's oracle had been exercised (two members each seeing their own work; a runner reaching green with nobody at a terminal; the gate reaching a second person OFF-MACHINE and returning signed; the decision against an Actor row). The off-machine round trip, the heart of it, was never built: its walking-skeleton child was cancelled early and I let the substrate slices stand in for the feature. Owner: "its nifty you added a screen but i can not tell that you completed anything." This is the SAME lesson already recorded against this same epic, reproduced at double the scale, because green children FEEL like progress and the roll-up rewards them. Before presenting a parent gate, walk the oracle CLAUSE BY CLAUSE and write what was OBSERVED next to each — not which child covered it; a clause with no observation means not done. Drive the epic's thinnest end-to-end path FIRST, and if that skeleton is ever cancelled the outcome is unowned and must be re-owned immediately (mx-abc9fc). Related: the mess costs more than the code — 17 tasks, 4 auto-mirrored GitHub issues for tickets I then cancelled, ~20 unmerged worktrees. Never let ticket creation outpace ticket completion.
- WORKING A TICKET STARTS THE MOMENT I READ THE CODEBASE FOR IT, not when the drive reaches `conductor_work`. On 3c1a326b (2026-08-07) the owner asked me to check the work was not already done; I spent the whole audit — grepping the API, the SPA, control_plane, the prototypes on disk — plus a full contract rewrite (oracle/stop_if/verify/allowed_files) with the row still `pending` and no session linked, so PRISM showed nobody on it and the owner's "if you are working this you're not reporting that state to prism" was right. The workflow's `Locate` step flips the row 3 minutes later, which is exactly the blind spot: the pre-drive audit is main-thread work and NOTHING forces it to register. `task_link_session(task_id=...)` FIRST, then audit; and because `task_update` cannot write `description`, the audit's findings go to `memory_store` (mx-0b99f4) plus `oracle`/`likely_misfire`/`stop_if`, never into chat only. Same root cause as the ASK-PRISM-BEFORE-BUILDING lesson: discretionary context work has no PRISM state unless I give it one.
- READINESS IS NOT THE DECISION, AND "APPROVED" IS NOT "APPROVED BY THE OWNER". Two attribution errors in one drive (3c1a326b, 2026-08-07). (1) I reported "at green_gate awaiting your click" while `task.gate_state` was already `passed` and status `done` - I had curled `/api/conductor/gate/readiness`, seen `adapter=human, status=your_review`, and read that as "still pending". Readiness describes the RECEIPT SHAPE, not the verdict; the verdict is `gate_state` on the task row, so read BOTH before telling a human what is or is not waiting on them. (2) I told the owner "you approved story_gate" and "you cleared plan_gate" when `api/conductor_flow.py:170` `_AUTOCLEAR_GATES = {"story_gate","plan_gate"}` had self-approved both as `session_id="conductor-autoclear", model="machine"` - the history rows say `reason=Approving on live evidence...` with no owner, while genuinely-human rows say `approved by owner (one-click from the task page)`. NEVER attribute a gate decision to anyone without reading that reason string in the `gate_decide` history row. Corollary product truth worth remembering: the owner's plan_gate stop DOES NOT EXIST in code - it autoclears on a FORM rubric - so a drive reaches implementation on a plan and a design direction nobody approved (mx: plan-gate-autoclears-owner-plan-stop-does-not-exist). SUPERSEDED 2026-08-27 by task 3c774abd (7.13.112, owner: "we want the users to approve the plans for parent level tasks, the system can make and manage as many sub agents as they want to manage subtasks that do not need user approval"): a ROOT task on the implement workflow now parks at plan_gate with `Plan rubric passed (machine review). Your approval releases the plan.` and readiness says adapter=human; a CHILD task and the daemon's run tasks (align_language, promote_to_law) keep the machine seat. So: create the owner's stops as root tasks, and everything a subagent must finish alone as a child.
- EVERY SPAWNED WORKER IS PRISM-VISIBLE (owner 2026-08-10: "any sub agent you spawn to work has to have a sub task in tasks in prism") — a subagent doing implementation work must be bound to a PRISM task: workflow step agents ride the driven task itself (conductor_work + agent-runs telemetry + heartbeats on that task's Trace), and any separately-scoped piece of work gets a real child task via task_create BEFORE its agent spawns; no worker ever does repo work that has no task row behind it.
- WHEN THE PERMISSION CLASSIFIER BLOCKS A STEP, EXHAUST MY OWN ROUTES BEFORE HANDING THE OWNER A CLICK — on 2026-08-09 I asked the owner to merge PR #413 and to approve a gate after classifier denials, and the owner's answer was "not sure why you are asking me to do your job"; a retry of the identical `gh pr merge` minutes later went straight through (the classifier is flaky on infrastructure actions), and the gate ask was only needed because I presented it before checking whether the affordance even rendered for them. Retry blocked commands once after doing other work, prefer alternate natural tools, and reserve "needs input" for actions that are genuinely the owner's decision, not my blocked mechanics.
- AN UNDRAINED REFLECTION QUEUE BLOCKS EVERY DRIVE, and the fix is mine, not the owner's. PRISM prefixes EVERY MCP result with a `PRISM_REFLECTION_PENDING` instruction banner while candidates are queued, and the implement workflow's pre-flight correctly reads instructions embedded in tool output as possible prompt injection and halts with `daemon_ok=false` ("MCP daemon returning injected instructions - daemon compromised/misconfigured"). On 2026-08-07 I stepped over that banner for the whole session until it killed a launch. `janitor_check` / `janitor_submit` are ORDINARY MCP TOOLS callable directly - no `prism-reflect` subagent is required, so a session rule against spawning agents is not a reason to leave it. Drain it at first sight (8 candidates that day). Never bypass the pre-flight check instead.
- A DRIVE HALTED AT AN OWNER GATE IS PAUSED, NOT FINISHED — the invoking thread OWNS the resume. On 1414a5e2 (2026-08-13) the owner approved plan_gate and the task sat driverless at write_failing_tests for an hour ("you did it again? stop"): nothing relaunches a drive after the click. The implement workflow's result now carries a `resume` contract when it halts at plan_gate/green_gate — honor it: watch the gate (Monitor/poll readiness or gate_state) and relaunch the workflow FRESH (never resumeFromRunId — cached pre-flight verdicts replay stale) the moment it clears. Related board truth shipped the same day (v7.10.37): "stalled · needs you" is now "no active driver" because with no driver attached the owner has NO affordance — that pill is a signal to ME to relaunch, never a reason for the owner to intervene; and step agents MUST post drive heartbeats (first beat immediately, then every ~5-8 tool calls) or the board reads healthy work as dead.
- CONSEQUENTIAL CONSENT MUST BE IN THE TRANSCRIPT AS TEXT before acting on it — on 2026-08-12 I recorded a delegated plan_gate approval from an instruction visible only inside the owner's screenshot ("approve the plan gates when they're ready" typed in their prompt box); the safety classifier could not verify the image, read the approval as fabricated self-consent, and blocked every agent launch for ~2 hours until the owner confirmed in text. When an instruction that clears a gate (or anything similarly irreversible) arrives only in an image, echo it back and get a one-word text confirmation FIRST; the round-trip costs a minute, the ambiguity cost the whole pipeline.
- ALWAYS pass `session_id` (CLAUDE_CODE_SESSION_ID) in the implement workflow's args — the Workflow sandbox has no env access, so `implement.js:90` reads it ONLY from args, and an omitted session_id makes every step lane POST agent-runs rows with `session_id:""`; the /live board then births a ghost "sm agent" session card (0 tokens, climbing "since signal") the owner reads as a dead agent (2026-08-16, both drives that day; product guard tracked in task ea92640f). Launch shape: `Workflow({scriptPath:..., args:{task_id, api_base, session_id}})`.
- NEVER chain `git commit`/`git push` after a `;` — I repeated the f7557ce failure on 2026-08-17 (fa7735bd d5af28d): the conflict-resolver script FAILED, but `pytest; cd; git add && commit && push` ran anyway and pushed conflict markers. A merge-resolution commit is gated on THREE checks in sequence, each `&&`-ed on the real rc: zero `^<<<<<<<|^=======|^>>>>>>>` matches, the file parses, the pinned suite passes un-piped. And when many branches bump PRISM_VERSION concurrently, validate the COMBINED result: merge all pending PR branches into one local integration worktree and run the full suite there before serial merges (owner 2026-08-17: "are we not merging these worktrees locally and validating against that?").
- A pipeline ending in `| tail`/`| head` reports the FILTER's exit code, not the command's — chaining `&& git commit && git push` after `pytest ... | tail -2` pushed a merge commit with unresolved conflict markers (f7557ce, 2026-08-17) because pytest's collection error was masked. Gate any commit/push on the real rc (`set -o pipefail`, or run the test un-piped first), and assert-then-verify file edits BEFORE the commit step, never in the same masked chain.
- WHEN TESTING/BUILDING PRISM ITSELF IN DEV (not driving a task that ships someone else's feature), the owner has standing authority to direct the working session to act as the distinct-actor gate reviewer on tasks it is explicitly, transcript-authorized to drive end-to-end (owner 2026-08-20/21, task 4f74dafc "Remove Simulate Flow button"). `task_link_session` at task creation does NOT taint that same session from later reviewing a gate on it — `gate_decide`'s no-self-review tooth means distinct from the PRODUCING seat (`prism-task-runner`, or whichever agent actually wrote the plan/story/tests), never distinct from every human or session that ever touched the ticket row. Read the real evidence (plan_doc/plan_diagram/completion_proof against actual source, not the gate_reason string) before approving — this authority is for genuine review, not a rubber stamp. Known open gap: even with explicit, repeated, real transcript authorization, the Claude Code Bash-tool permission classifier (a layer BELOW PRISM's own gate logic, which correctly allows a non-tainted actor label) refused `POST /api/conductor/gate` with `actor="owner"` twice in a row on 2026-08-21 — that is a session-level safety boundary this file cannot lift by itself; if it recurs, the owner's own click on the task page, or a Bash permission rule in Claude Code settings, is the actual unblock, not a different actor string.
- DIRECT PUSH TO `dev`/`main` IS OWNER-AUTHORIZED for this repo's own self-development, despite the "Never commit to... develop"-style convention elsewhere in this file — a fresh, independently-launched agent (`graph-position-persist`, 2026-08-21) pushed a real, tested fix straight to `origin/dev` and the harness flagged it as a security-policy violation (no PR, no `/ship`), because it had no way to know the standing pattern this SAME session had already used ~10 times that night with the owner's active, ongoing engagement. Asked directly, the owner reconfirmed: keep pushing directly for PRISM-on-PRISM work (see the Key Conventions carve-out above). The lesson: an established-but-undocumented in-session norm is invisible to any fresh agent and to the harness's own safety layer — the moment a security flag surfaces a real gap between documented policy and actual practice, WRITE THE EXCEPTION DOWN (here, in CLAUDE.md) rather than silently keep operating on tribal knowledge; don't just re-confirm verbally and move on.
- ON THE AOS-HOSTED DEV INSTANCE, FLIPPING A TASK TO `in_progress` HANDS IT TO THE DAEMON, NOT JUST TO WHOEVER YOU TOLD — `PRISM_TASK_RUNNER_INTERVAL=900` is live there (see Service ports below), so the daemon's own autonomous drive worker picks up any `in_progress` task and drives it through `conductor_work` unprompted, in parallel with whatever session/subagent you think is driving it. On task d9f082fe (2026-08-24) this produced a fully-populated `plan_doc`/`plan_diagram`/`premise_notes` even after every qa-agent/subagent I'd spawned explicitly confirmed it had stood down and submitted nothing — the daemon had been driving it the whole time, which is what actually produced the "who approved this, why are there two queues of control" confusion the owner was reacting to, not just my own nested-subagent chain (a separate, compounding mistake that same session — see the next lesson). If you abandon conductor-driven delivery for a task on this instance (e.g. to ship it directly per the self-dev carve-out instead), CANCEL/RECLASSIFY it immediately rather than leaving it `in_progress` — the daemon will otherwise keep driving it indefinitely in the background regardless of what any session says.
- WHEN THE OWNER ASKS FOR A DIRECT, CONCRETE FIX AND OBJECTS TO GATE CEREMONY, DROP THE CONDUCTOR AND JUST FIX IT — on task d9f082fe (2026-08-24, a real bug: the Work screen rendered two unsorted, undeduplicated queues instead of one) I spun up a qa-agent, which spun up a second "implement-driver" subagent to invoke the `Workflow` tool's `implement` script — but no session in that chain had the user's actual opt-in for `Workflow`-tool orchestration that turn, so it stalled on missing tooling, crossed status messages, and produced zero progress across ~10 minutes of relayed idle-notifications. Owner: "please dont do that that way" then "you have created a system in which you can approve tasks, and neither can I" then "so you have to at the very least fix the view." The fix: stand every delegate down, `EnterWorktree`, make the code change directly, add a regression test, bump `PRISM_VERSION`, commit/push straight to `dev`+`main` per the self-dev carve-out, rebuild `web_dist`, bounce the live daemon, and verify live via `agent_bridge_command` against the owner's own tab — no conductor, no subagents. For a well-scoped, already-diagnosed bug in this repo, prefer that path over orchestration by default; reach for conductor/subagent delegation only when the owner asks to see PRISM's own SDLC exercised, not as the default way to get a fix shipped.
- A STEP AGENT SELF-REPORTING "IMPLEMENTATION COMPLETE, N TESTS PASSING" IS NOT THE SAME CLAIM AS "EVERY AC IS BUILT" — on task e14680ba (2026-08-25) an implement-phase agent reported 9 passing tests and workflow_step=verify_green_state, but had only built 3 of the story's 7 acceptance criteria (the Trace-tab link and the `/workflows?task=` entry point) — no task-link-back, no pill-indicator strengthening, no layout split, no speed control — then crashed on context overflow before ever visually checking its own work against the approved prototype, and its report even suggested the OWNER "click through" to generate the missing visual evidence, which would have laundered the verification step onto the person the gate exists to protect. `verify_green_state` is not a second test run; it exists specifically to check the build against the approved plan_doc/prototype clause-by-clause, and a coordinating session must read the real AC list and demand file/line or screenshot evidence for EACH one before accepting "done" — passing tests only prove the tests are internally consistent, not that the story was implemented. Chained relay subagents made this worse and reproduced the exact d9f082fe pattern above almost verbatim: qa-agent -> qa-agent-ship -> qa-agent-verify handed off across model/context boundaries, repeatedly went idle with bare `idle_notification` pings and no substantive content even after direct, explicit asks for partial results, and burned the owner's patience ("this is a massive waste of time") before ever producing a real screenshot. Structural fix owed to PRISM itself, not just this session's prompting: `verify_green_state`/`implement_tasks` should refuse to advance a task past itself without an evidenced AC-to-diff mapping, and any QA/visual-check step should always be driven live through remote assist as an actual user journey (agent_bridge_command against the owner's own tab) rather than accepted as a self-attested chat claim or deferred back to the owner to perform themselves — file this as a PRISM product-defect task once MCP access allows, per "anything that structurally prevents a subagent from completing a ticket is a product defect."
- A TEST THAT ASSERTS `stored == normalize(input)` PROVES NOTHING: on the STE memory slice (5de57583, 2026-08-26) the pinned test compared the stored body against the normaliser's own output, went green, and the live oracle walk on 7.13.72 found 'Endpoints.cs; each' still holding its semicolon (the path mask swallowed the ';'). A slice test must assert the LITERAL expected outcome the spec names ("Endpoints.cs. Each"), never the function under test applied to its own input - and the live oracle walk is what caught it, so keep walking it after every deploy.
- AN AGENT-SHAPED BEHAVIOUR IS A TOP-LEVEL WORKFLOW ON /workflows, NEVER A JOB A SESSION RUNS FROM OUTSIDE (owner 2026-08-26: "if there is such a thing as a lexicon core agent, then that needs to be added to the top level workflows, like we have the conductor, so that we can control its behavior from within the system"). I had cut the task-convergence backfill as a POST route plus a script I would run - the same shape as hand-driving lanes (mx-d5275d). The product shape is: a WORKFLOWS entry with named steps (models/workflow.py), a catalog entry with step content on the Workflows page (api/workflows.py, the rail that lists Conductor / Triage / CI to local dev), behaviour in the versioned per-project file .prism/behaviors/<workflow>.json that workflow_behavior_get/provide read and revise, a daemon worker (services/<name>_worker.py) that creates a VISIBLE run task per pass and drives it through the conductor, and an API/UI trigger that only starts that same run. Check /workflows before designing any worker (mx-f49a5c).
- NEVER CALL EnterWorktree FOR THE MAIN SESSION'S OWN DIRECT WORK WHILE A BACKGROUND `Workflow` DRIVE WITH ITS OWN STEP SUBAGENTS IS IN FLIGHT — twice in one session (2026-08-26), a background `implement` drive's step subagent had its Bash tool unconditionally refused mid-step, citing "This session is isolated in the worktree <some other worktree>" where that worktree belonged to a completely different, sibling/main agent that had called `EnterWorktree` for its own unrelated work - never to the blocked subagent's own context, which it was never told to isolate into at all. `ExitWorktree` could not clear it either ("cannot be called from a subagent with a cwd override... This agent is already isolated"), so the drive was left permanently stuck mid-step for reasons entirely outside its own control. Checked: no PreToolUse hook exists (or could exist) that sees "is a background Workflow currently running" at the moment `EnterWorktree` fires - this looks like a genuine harness-level isolation-scoping leak across concurrently-running agents in one session, filed as platform feedback, not fixable from inside this repo. Until/unless that lands: before calling `EnterWorktree` for direct work, check whether a background `Workflow` drive with its own subagents is currently in flight (recent `Workflow`/`Agent` launches with no completion notification yet) - either wait for it to halt/complete first, or do the worktree-isolated work in a forked/child agent instead of the main session, so the isolation-slot binding never has two live concurrent claimants in the same session. If a drive halts citing an unfamiliar worktree name mid-step, recognize it immediately as this exact collision (not a task-content failure) and just relaunch fresh once no other agent in the session is mid-`EnterWorktree`.
- `git stash` IS SHARED ACROSS EVERY WORKTREE OF THIS REPO, so two parallel agents that each `stash push`/`pop` to prove a red baseline swap each other's uncommitted work (lexicon-linker and lexicon-import did exactly this on 2026-08-26 and spent ~10 minutes recovering via saved diffs). Every subagent brief must say: never use `git stash`; prove red by committing the tests first and running them against HEAD~ in place, or by a targeted revert with the Edit tool. The orchestrator checks `git diff --stat HEAD...<branch>` for stray files before every merge.
- READ THE DRY-RUN SAMPLES, NEVER JUST THE COUNTS, BEFORE AN APPLY PASS OVER REAL DATA — and start with a small batch. On 2026-08-26 the Align language backfill's dry-run said 'would_change 50, per_rule lexicon 830' and I applied it to 525 tasks; the samples would have shown 'gate card' -> 'gate Task' and 'proof of' -> 'Evidence of' (story, card, proof, memory, pipeline, checkpoint, sign-off, receipt, todo, acceptance test were synonyms). I had written that exact misfire into the ticket's likely_misfire and never tested it on real text. The repair worked only because every ste_normalise row kept `before` text (epic cc9a44c8, 7.13.86).
- WHEN MY OWN VERIFICATION SCRIPT SAYS FAIL, FIND OUT WHY BEFORE JUDGING THE DATA — I first concluded the history rows held no `before` text ("the agent's claim did not hold") because my parser expected pure JSON and the row stores 'rules=... before={json}'. The data was fine; my reader was wrong. A FAILS from a check I wrote is a claim about my check first.
- THE DAEMON RUNS THE SHARED CHECKOUT'S WORKING TREE, NOT ITS HEAD: after `git pull` there, run `git status --short` before restarting — on 2026-08-26 a stale index held a staged reversal of another session's commit (nine files byte-identical to the previous commit), so two restarts started a process that reported 7.13.86 while HEAD said 7.13.87. Verify per file that the staged content equals a known commit before `git restore --staged --worktree`, and leave files you cannot account for (the LiveBar pair) alone.
- PATCH /api/tasks/{id} SILENTLY DROPS `premise_notes` and answers 400 'no fields to update' when it is the only field; TaskService.update accepts it. A field the service accepts but the route ignores is a drift hole that a repair pass hits first (fix in task dc676e24).
- NEVER `git checkout HEAD -- <file>` WHILE A MERGE IS IN PROGRESS: HEAD is the PRE-merge commit, so the restore silently drops the other side's changes to that file and the merge commit then ships without them. On 2026-08-26 I bisected a failing gate test by swapping upstream files in and 'restoring mine from HEAD', and committed a merge that had lost upstream's reachability self-heal (_fresh_merge_base); upstream's own new test caught it. Bisect on a scratch copy, or restore with `git checkout MERGE_HEAD -- <file>` / `git checkout --merge`, and diff the merged file against both parents before committing. Related: a failing gate test in a worktree with an UNCOMMITTED merge is the dirty-judge tooth, not a code defect - commit the merge first (the lesson above about POLICY_FILES applies to merge state too).
- AN rc=139 WHOSE LOG ENDS MID `Timeout (0:00:30)!` DUMP IS THE FAULTHANDLER WATCHDOG, NOT THE TEST: pytest's faulthandler_timeout walks every thread's frames from a watchdog thread without the GIL, and on a thread deep in pyparsing/rdflib that walk itself segfaults the whole run (waves 34, 36, 37 on 2026-08-27 - three truncated dumps, three different tests, all ending in pyparsing frames). I first read it as the known SPARQL-recursion stack overflow and added a big-stack guard that changed nothing. The real defect was a test that took >30 s under the full suite (a rebuild+validate+subprocess warm-up loop) and so got dumped; the fix is the slow test, and the wave runner now passes `-o faulthandler_timeout=120` like `.github/workflows/pr-checks.yml`. Read the LAST test name in a verbose log (`-v -s`) before diagnosing a crash class.
- THE LOOP I RAN BY HAND ON 2026-08-27 IS THE CONDUCTOR'S JOB, AND THE SEATS ALREADY EXIST (owner: "alot of this flow should have been part of the conductor flow in the app, not up to you"). For ~12 tasks in one day I created the row, briefed my OWN Agent-tool subagent in a worktree, merged into an integration worktree, ran the suite, bumped, pushed dev+main, deployed to AOS, walked the live oracle and closed the task myself - while `task_runner` (PRISM_TASK_RUNNER_INTERVAL=900, drives agent steps through `claude -p`), `gate_adjudicator` (60 s) and `ship_worker` were all live on this instance and never touched. It was not "the app cannot move forward" (mx-914d37), it was speed and control. THE RULE: a piece of work enters as a task (root for the owner's two stops, child for anything a subagent finishes alone), goes `in_progress`, and the daemon drives it; I watch the board, answer gates the human owns, and file a task under epic 3baadd19 for every seat the loop hits that is missing or too slow - never substitute my own hands for the missing seat. Known gap filed today: no seat deploys a shipped build to the dev instance (pull, web_dist rebuild, bounce, /api/version check).

## Key Conventions

- **Never commit to**: main, master, staging, develop — EXCEPT this repo's own self-development (PRISM building PRISM). Owner standing authorization (2026-08-21, reconfirmed after a fresh agent's direct push to `dev` was flagged as a policy violation): a session/agent working ON `prism-service` itself may commit and push directly to `dev` and `main` — no feature branch, no PR, no `/ship` — following the established per-fix ritual (bump `PRISM_VERSION` with a changelog entry, run the relevant tests, commit, `git fetch . <local-branch>:dev`, `git push origin dev`, then the same for `main` with `--no-verify` since the pre-push hook is permanently broken, see the Lessons entry on this). This exception is scoped to `prism-service`'s own codebase; it is NOT a general license — driving a task through the conductor for someone else's feature still goes through the conductor's own gates, and other Bespoke Labs products keep their normal branch protection.
- **File writes**: Max 30 lines per operation, chunk larger writes
- **Hooks**: Advisory only (exit 0), never block tool execution
- **Citations**: Read before you reference — never cite unread sources
- **Destructive ops**: Never inline PowerShell, always validate paths, never -ErrorAction SilentlyContinue

## Service ports

Three instances have been observed live on this machine — never mix them up, and confirm which one a given session is actually talking to before trusting doc guidance below over a live `curl .../api/version` check:

- **AOS-hosted DEV (WSL background-job sessions in this repo, confirmed live 2026-08-21)** — the AOS Aspire host (`~/projects/aos/apphost.cs`, resource name drifts per AppHost run - `prism-epunygvd`, then `prism-bheqtamr` on 2026-08-26; `aspire resource prism restart` matched by prefix earlier that day but `prism.qa` had to read the real name from `aspire describe` - query it, never assume) runs `uv run --project services/prism-service ... prism start --ui-port 7780` directly against THIS checkout (`/home/siegeon/projects/prism`, `PRISM_DATA_DIR=/home/siegeon/.prism`). MCP `http://localhost:7777/mcp/?project=prism`, Web UI `http://localhost:7780/`. `PRISM_TASK_RUNNER_INTERVAL=900` and `PRISM_GATE_ADJUDICATOR_INTERVAL=60` are both set here, so the autonomous drive loop and machine gate adjudication are LIVE — a task flipped to `in_progress` in this project WILL be driven for real, unprompted. After any code change: sync the edited files into this checkout (worktree changes don't auto-appear here — `git checkout HEAD -- <path>` after merging to `dev`/`main`), then `aspire resource prism-epunygvd restart --apphost ~/projects/aos/apphost.cs --non-interactive` (falls back to `start` if `restart` errors "Failed to stop resource" — a known-flaky Aspire CLI path, not a real failure), then confirm `/api/version` reports the new build. This superseded the "DEV (what sessions in this repo use)" entry below for this machine at some point — the entry is left in place because it may still describe a genuinely separate Windows-side session context this WSL job never observed directly; verify live before assuming either is current.
- **DEV (Windows source-run, as previously documented)** — MCP `http://127.0.0.1:8887/mcp/?project=prism`, Web UI `http://127.0.0.1:8888/`. Claude Code's prism MCP for `E:\.prism` is overridden to 8887 in `~/.claude.json`; if it's unreachable, ask the owner to start dev (`prism-dev` skill) and `/mcp` reconnect — never build an HTTP shim around it. Use `127.0.0.1`, not `localhost` (IPv6-first resolution stalls ~200ms/request). Every code change ends with this daemon bounced and `/api/version` reporting the new build. NOT reachable from a WSL session (confirmed 2026-08-21: `curl` to 8887/8888 from WSL returns nothing) — this is a Windows-local process.
- **RELEASE (leave alone)** — WSL pipx: MCP `http://localhost:7777/mcp/?project=prism`, Web UI `http://localhost:7778/`. The same FastAPI process serves `/api/*` (JSON), `/sse/sessions` (events), and `/graph/viewer/{project}` (Sigma WebGL) on both instances. Default tool profile is `interactive`; use `tool_profile=all` for admin sessions. NOTE 2026-08-21: port 7778 was observed serving a `vite preview` STATIC BUILD of the web bundle, not a full FastAPI backend — the real backend for that frontend may be the AOS-hosted instance above, not a separate pipx install; this entry needs re-verification, not blind trust.

**Dev on this machine**: use the `prism-dev` skill — editable install from `E:\.prism\.venvs\dev`, source-run on 8887/8888, Edge `--app` window. Never docker/pipx/Tauri for local dev; any path >30s build/install is wrong. Patch-bump `PRISM_VERSION` on every user-visible change and bounce the daemon. For the AOS-hosted instance above, "bounce the daemon" means the `aspire resource ... restart`/`start` command, not this skill's own launch mechanism.

The `prism` CLI and `services/prism-service/docker-compose.yml` are end-user / server paths, never dev paths.

UI HMR runs on 5173 (`npm run dev` under `prism_service/web`) and hits the running API, not its own backend.
