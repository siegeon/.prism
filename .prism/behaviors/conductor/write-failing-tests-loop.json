{
  "id": "write-failing-tests-loop",
  "fsmId": "pipeline",
  "botId": "conductor",
  "name": "Write failing tests (Observe-Reason)",
  "version": 1,
  "steps": [
    {
      "id": "loop",
      "kind": "http-callback",
      "url": "${prismBackendUrl}/api/workflows/steps/reason-loop?project=${project}",
      "body": "{\"persona\": \"qa\", \"prompt\": \"Draft a failing test (do NOT write it to disk, this is a DRAFT only) for this task: ${taskHint}\\n\\nThe test must exercise the story's acceptance criteria and currently fail because the behavior doesn't exist yet.\\n\\nTHE FAILURE MUST BE A GENUINE ASSERTION FAILURE (pytest exit code 1), NEVER an uncaught exception, import error, or a raw lookup that raises before any assertion runs (e.g. str.index() on a missing substring, dict[key] on a missing key, or importing a module/symbol that does not exist yet). This repo's red-gate verifier explicitly refuses to count a collection error or an unhandled exception as red demonstrated (pytest exit code 2 or 4) - only rc==1 (tests ran and genuinely failed an assertion) counts. If the target module or symbol does not exist yet, do NOT import it directly; either import the existing parent module and assert on the absence/current shape of the not-yet-added capability (e.g. `assert hasattr(mod, 'new_thing'), \\\"not implemented yet\\\"`), or use `assert needle in haystack, \\\"reason\\\"` style checks (never `haystack.index(needle)`) so a missing needle raises a clear AssertionError instead of an uncaught ValueError.\\n\\nReturn the test code, the file path it would live at, and why it's expected to fail.\", \"json_schema\": {\"type\": \"object\", \"properties\": {\"test_code\": {\"type\": \"string\"}, \"test_file_path\": {\"type\": \"string\"}, \"expected_failure_reason\": {\"type\": \"string\"}}, \"required\": [\"test_code\", \"test_file_path\", \"expected_failure_reason\"]}, \"rubric\": \"\", \"model\": \"haiku\", \"max_budget_usd\": 0.5, \"max_turns\": 4, \"task_id\": \"${taskId}\"}",
      "timeoutSeconds": 120
    }
  ]
}
